import { styled } from '@mui/material';

const StorySlot = styled('div')({
  borderRadius: 'var(--radius)',
  borderWidth: '1px',
  borderStyle: 'dashed',
  borderColor: 'hsla(266, 100%, 64%, 1)',
  padding: 'var(--spacing-4) var(--spacing-24)',
  color: 'hsla(266, 100%, 64%, 1)',
  textAlign: 'center',
});

export default StorySlot;
